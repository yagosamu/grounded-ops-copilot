services:
  postgres:
    image: postgres:16.10-alpine
    environment:
      POSTGRES_DB: ingestion_test
      POSTGRES_USER: ingestion_test
      POSTGRES_PASSWORD: ${TEST_POSTGRES_PASSWORD:?required}
    ports:
      - "127.0.0.1::5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ingestion_test -d ingestion_test"]
      interval: 1s
      timeout: 2s
      retries: 30
